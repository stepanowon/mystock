# ────────────────────────────────────────────────────────────────
# MyStock 환경 변수
#
# [로컬 개발] 이 파일을 .env 로 복사 후 값을 채워주세요.
#   cp .env.example .env
#
# [Netlify 배포] .env 파일은 배포에 포함되지 않습니다.
#   Netlify 대시보드 → Site configuration → Environment variables 에서
#   아래 변수를 직접 등록하세요.
# ────────────────────────────────────────────────────────────────

# Firebase — 필수
# Firebase Console → 프로젝트 설정 → 일반 → 내 앱 → SDK 설정 및 구성
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# KIS API (한국투자증권) — 선택
# 설정 시 한국 주식 시세를 KIS API에서 직접 조회합니다.
# https://apiportal.koreainvestment.com
VITE_KIS_APP_KEY=
VITE_KIS_APP_SECRET=
